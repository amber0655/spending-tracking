import { useState } from 'react'
import { AppHeader, BottomNav } from '../components/AppShell'
import { Icon, type IconName } from '../components/Icon'

type Insight={icon:IconName;title:string;value:string;tone:'good'|'alert'|'neutral';description:string;wide?:boolean}
const insights:Insight[]=[{icon:'coffee',title:'Dining Behavior',value:'+15.4%',tone:'alert',description:'You spent 15% more on dining than last month. Consider moving three meals to home cooking to save $140 next month.',wide:true},{icon:'arrowUp',title:'Savings Momentum',value:'+8.2%',tone:'good',description:'Your savings rate improved for the third month in a row.'},{icon:'calendar',title:'Subscription Check',value:'$34/mo',tone:'neutral',description:'Two subscriptions have not been used recently.'},{icon:'car',title:'Transport Trend',value:'-12.0%',tone:'good',description:'Public transit helped reduce transport costs this month.'}]
export function InsightsPage(){const[period,setPeriod]=useState('Monthly');return <div className="min-h-screen bg-[#f7f9fb]"><AppHeader/><main className="mx-auto max-w-6xl px-5 pb-28 pt-24 sm:px-8"><p className="eyebrow text-[#71809a]">Financial overview</p><h2 className="mt-2 text-3xl font-semibold">Smart Insights</h2><div className="segmented-control mt-4">{['Weekly','Monthly','Yearly'].map(p=><button key={p} onClick={()=>setPeriod(p)} className={period===p?'is-active':''}>{p}</button>)}</div><section className="insights-grid mt-6">{insights.map(i=><article key={i.title} className={`insight-card ${i.wide?'insight-card--wide':''}`}><div className="flex justify-between"><div className={`insight-icon insight-icon--${i.tone}`}><Icon name={i.icon} className="size-6"/></div><span className={`insight-value insight-value--${i.tone}`}>{i.value}</span></div><h3 className="mt-5 text-xl font-semibold">{i.title}</h3><p className="mt-2 text-[15px] leading-6 text-[#5c5d64]">{i.description}</p></article>)}</section></main><button aria-label="Ask Aura AI" className="ai-fab"><Icon name="sparkles" className="size-7 fill-current"/></button><BottomNav active="insights"/></div>}
