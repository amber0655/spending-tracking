import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { AppHeader } from '../components/AppShell'
import { Icon } from '../components/Icon'

function Receipt(){return <div className="receipt-paper"><div className="receipt-brand">AURA MARKET</div><div className="receipt-rule"/>{['Coffee','Avocado Toast','Market Salad','Sparkling Water'].map((item,index)=><div key={item} className="receipt-line"><span>{item}</span><span>${[4.5,12,14.5,3.25][index].toFixed(2)}</span></div>)}<div className="receipt-rule"/><div className="receipt-line receipt-total"><span>Total</span><span>$34.25</span></div></div>}
export function ReceiptScanPage(){const[captured,setCaptured]=useState(false),[flash,setFlash]=useState(false);const navigate=useNavigate();return <div className="scan-page"><AppHeader/><main className="scan-stage"><div className="scan-texture"/><div className="scan-content">{!captured?<><p className="scan-instruction">Align receipt within frame</p><div className="viewfinder"><span className="corner corner--tl"/><span className="corner corner--tr"/><span className="corner corner--bl"/><span className="corner corner--br"/><Receipt/></div><div className="capture-controls"><button aria-label="Toggle flash" onClick={()=>setFlash(!flash)} className={`scan-side-button ${flash?'is-active':''}`}><Icon name="flash" className="size-6"/></button><button aria-label="Capture receipt" onClick={()=>setCaptured(true)} className="capture-button"><span/></button><button aria-label="Choose from gallery" className="scan-side-button"><Icon name="gallery" className="size-6"/></button></div></>:<div className="scan-preview"><div className="relative"><Receipt/><div className="scan-line"/></div><div className="mt-5 flex justify-center gap-2 text-[#6cf8bb]"><Icon name="sparkles" className="size-5 fill-current"/><span>Receipt data extracted</span></div><div className="mt-7 grid grid-cols-2 gap-3"><button onClick={()=>setCaptured(false)} className="preview-secondary">Retake</button><button onClick={()=>navigate('/expenses/new')} className="preview-primary">Confirm</button></div></div>}</div></main></div>}
